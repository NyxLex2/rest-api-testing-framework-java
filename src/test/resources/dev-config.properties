base.url=http://3.68.165.45
request.timeout.seconds=5
response.timeout.seconds=5
env=dev

request.timeout=30000
connection.timeout=10000

max.retries=3
retry.delay=1000

# Logging settings
log.level=DEBUG
log.requests=true
log.responses=true

# Schema validation
validate.schemas=true

# Performance thresholds (in milliseconds) - relaxed for dev
max.response.time=10000
max.create.response.time=10000
max.update.response.time=10000
max.delete.response.time=5000
max.get.response.time=5000

# Test data settings
generate.unique.data=true
cleanup.test.data=true
